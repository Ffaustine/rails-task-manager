<h1><%= @task.title %></h1>
<ul>
  <li><%= @task.details%></li>
  <li><input type=checkbox <%= @task.completed == true ? "checked" : "unchecked" %>/>
  <%= @completed %></li>
</ul>

<%= link_to "Edit this task", edit_task_path %>

<%= link_to "Back to list", tasks_path %>
